#!/usr/bin/perl -w

my $config
    = {
       files => [
		 './configure.ac',
# 		 './test-dist',
		 './tests/run',
		],
       labels => {
		  package => '',
		  major => '',
		  minor => '',
		  micro => '',
		  label => '',
		  email => '',
		 },
      };

return $config;

